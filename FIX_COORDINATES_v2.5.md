# 2D互作图修复日志 v2.5 (坐标系最终修正)

## 🐛 问题根源分析

用户反馈“箭头乱指”且“疏水圆圈位置偏移”。通过观察 v2.4 版本的 Debug 截图，我们发现：
- 所有的标注（绿色圆圈、箭头终点）相对于背景分子图都有一个**径向偏移**。
- 这表明 RDKit 计算的像素坐标 `(x,y)` 与 Matplotlib 显示背景图时的坐标系不重合。

**原因**: `ax.imshow(..., extent=[0, w, 0, h])` 参数强制定义了图片的数据范围。
- 在 `origin='upper'` 模式下，`extent` 的 Y 轴范围定义可能导致了意料之外的缩放或位移，导致背景图和前景标注点错位。

## 🛠️ v2.5 修复方案

### 1. 移除 `extent` 强制映射 ✅
- **修改**: 删除了 `imshow` 中的 `extent` 参数。
- **效果**: 让 Matplotlib 直接使用图片的**原始像素坐标系**。这样，RDKit 返回的原子像素坐标 `(100, 100)` 就精准对应图片上的 `(100, 100)` 像素点，消除了任何缩放或平移误差。

### 2. 移除 Debug 干扰 ✅
- **修改**: 注释掉了 v2.4 引入的灰色原子名标注。
- **效果**: 恢复图面整洁，避免用户觉得“太乱”。

### 3. 保留之前的增强 ✅
- **原子名匹配**: 继续保留大小写不敏感和去空格逻辑，防止匹配错误。
- **箭头方向**: 继续保持从 Badge 指向配体的正确方向。
- **疏水圆圈**: 继续保持无连接线、半透明的清爽风格。

## 🚀 预期效果

现在，箭头尖端应该**精准地**指在原子中心，疏水圆圈也应该**完美覆盖**在对应的原子或基团上，不再有“漂移”现象。

## 📝 验证指令

```bash
python test_2d_diagram_v2.py --csv interactions.csv --ligand MOL
```

---

**更新日期**: 2025-12-15  
**版本**: v2.5 (Coordinate System Fix)
