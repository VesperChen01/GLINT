# 2D互作图修复指南 v2.4 (原子匹配修复)

## 🐛 核心问题诊断

用户反馈“疏水跟氢键作用与小分子结构完全对应不上”，这通常由两个原因导致：
1. **原子名不匹配**：PDB中的原子名（如 ` C1 `）与CSV中的（如 `c1` 或 `1`）不一致。
2. **原子索引错乱**：去除氢原子 (`RemoveHs`) 后，原子顺序改变，导致基于索引的映射失效。

## 🛠️ v2.4 修复方案

### 1. 智能原子名匹配 (Smart Matching) ✅
- **大小写不敏感**: 自动将所有原子名转换为大写 (e.g., `Ca` == `CA`)。
- **去除空格**: 自动去除前后空格 (e.g., ` C1 ` == `C1`)。
- **重复名检测**: 如果PDB中存在重复原子名（如所有碳都叫`C`），程序会打印黄色警告 `⚠️ Warning: Duplicate atom names found`。

### 2. 可视化调试层 (Debug Layer) 🔍
- **原子名标注**: 在生成的图中，每个原子旁边会显示其 PDB 原子名（灰色微小字体）。
- **用途**: 
  - 如果你看到图上的原子名（如 `C1`）标在正确的位置，但互作箭头指错了，说明 CSV 数据有问题。
  - 如果你看到图上的原子名标错了位置（比如标在氧上的叫 `C5`），说明 PDB 文件原子编号有问题。

### 3. 坐标系校准 📐
- 再次确认了 RDKit 绘图坐标系与 Matplotlib 坐标系的对齐，确保 (0,0) 原点一致。

## 🚀 如何验证

运行测试脚本：

```bash
python test_2d_diagram_v2.py --csv interactions.csv --ligand MOL
```

**观察生成的图片：**
1. 仔细看每个原子旁边的灰色小字（放大看）。
2. 确认灰色小字（原子名）是否与化学结构对应。
3. 确认互作标记（箭头/圆圈）是否指向了正确的原子名。

## ⚠️ 常见问题排查

如果图还是“乱”的，请检查：

1. **警告信息**: 运行脚本时是否有 `Duplicate atom names` 警告？
   - 如果有，你需要修改配体 PDB 文件，确保每个原子有唯一名字（如 `C1`, `C2`, `O1`...）。
2. **CSV内容**: 打开 CSV 文件，检查 `Ligand_Atom` 列。
   - 它是原子名（如 `C1`）还是索引（如 `1`）？目前代码只支持原子名。
3. **PDB文件**: 确保 PDB 文件中包含标准的 `ATOM` 或 `HETATM` 记录，且包含原子名列。

---

**更新日期**: 2025-12-15  
**版本**: v2.4 (Robust Atom Mapping)
